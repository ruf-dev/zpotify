// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: artist_songs.sql

package songs_q

import (
	"context"
	"database/sql"
	"encoding/json"
	"time"

	"github.com/google/uuid"
)

const listArtistFeaturedSongs = `-- name: ListArtistFeaturedSongs :many
SELECT v.id,
       v.title,
       v.created_at,
       v.duration_sec,
       v.file_path,
       v.file_id,
       v.artist_info,
       v.cover_file_path
FROM song_search_view_v3 v
WHERE EXISTS (SELECT 1
              FROM songs_artists sa
              WHERE sa.song_id = v.id
                AND sa.artist_uuid = $1
                AND sa.order_id != 0)
ORDER BY v.created_at DESC
LIMIT $2
`

type ListArtistFeaturedSongsParams struct {
	ArtistUuid uuid.UUID
	Limit      int32
}

type ListArtistFeaturedSongsRow struct {
	ID            int64
	Title         string
	CreatedAt     time.Time
	DurationSec   int64
	FilePath      string
	FileID        int64
	ArtistInfo    json.RawMessage
	CoverFilePath sql.NullString
}

// Features: tracks where the given artist appears but is not the primary
// artist (order_id != 0).
func (q *Queries) ListArtistFeaturedSongs(ctx context.Context, arg ListArtistFeaturedSongsParams) ([]ListArtistFeaturedSongsRow, error) {
	rows, err := q.db.QueryContext(ctx, listArtistFeaturedSongs, arg.ArtistUuid, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListArtistFeaturedSongsRow{}
	for rows.Next() {
		var i ListArtistFeaturedSongsRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.CreatedAt,
			&i.DurationSec,
			&i.FilePath,
			&i.FileID,
			&i.ArtistInfo,
			&i.CoverFilePath,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listArtistPrimarySongs = `-- name: ListArtistPrimarySongs :many
SELECT v.id,
       v.title,
       v.created_at,
       v.duration_sec,
       v.file_path,
       v.file_id,
       v.artist_info,
       v.cover_file_path
FROM song_search_view_v3 v
WHERE EXISTS (SELECT 1
              FROM songs_artists sa
              WHERE sa.song_id = v.id
                AND sa.artist_uuid = $1
                AND sa.order_id = 0)
  AND NOT EXISTS (SELECT 1
                  FROM playlist_songs ps
                           JOIN playlists_artists pa ON pa.playlist_uuid = ps.playlist_uuid
                  WHERE ps.song_id = v.id)
ORDER BY v.created_at DESC
LIMIT $2
`

type ListArtistPrimarySongsParams struct {
	ArtistUuid uuid.UUID
	Limit      int32
}

type ListArtistPrimarySongsRow struct {
	ID            int64
	Title         string
	CreatedAt     time.Time
	DurationSec   int64
	FilePath      string
	FileID        int64
	ArtistInfo    json.RawMessage
	CoverFilePath sql.NullString
}

// Singles: standalone tracks (not attached to any album playlist) where the
// given artist is the primary artist (order_id = 0).
func (q *Queries) ListArtistPrimarySongs(ctx context.Context, arg ListArtistPrimarySongsParams) ([]ListArtistPrimarySongsRow, error) {
	rows, err := q.db.QueryContext(ctx, listArtistPrimarySongs, arg.ArtistUuid, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListArtistPrimarySongsRow{}
	for rows.Next() {
		var i ListArtistPrimarySongsRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.CreatedAt,
			&i.DurationSec,
			&i.FilePath,
			&i.FileID,
			&i.ArtistInfo,
			&i.CoverFilePath,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

cache_path: proto_modules

deps:
  - github.com/googleapis/googleapis
generate:
  - inputs:
      - directory: "api/grpc"
    plugins:
      - name: go
        out: inernal/api/server/zpotify_api
        opts:
          paths: source_relative
      - name: go-grpc
        out: inernal/api/server/zpotify_api
        opts:
          paths: source_relative
      - name: grpc-gateway
        out: inernal/api/server/zpotify_api
        opts:
          paths: source_relative
      # Documentation + swagger
      - name: openapiv2
        out: pkg/docs/swaggers
      - name: docs
        out: pkg/docs